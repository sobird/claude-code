// Auto-generated stub — replace with real implementation
import  { FC } from 'react';

interface Props {
  onDone: (selection: string, modelAlias?: string) => void
}

export const UndercoverAutoCallout: FC<Props> = ({onDone}) => {
  return (
    <div>UndercoverAutoCallout</div>
  )
}
